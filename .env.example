# =================================
# WaHub Docker Environment Variables - EXAMPLE
# =================================
# Copia este archivo a .env y actualiza los valores según tu configuración

# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Production

# Application Ports
APP_HTTP_PORT=8080
APP_HTTPS_PORT=8081

# Database Configuration
DB_SERVER=sqlserver
DB_NAME=WahubManager
DB_USER=sa
DB_PASSWORD=TuContraseñaSegura123!
DB_PORT=1433
DB_TRUST_CERT=true

# Migration Settings
DB_AUTO_MIGRATE=true
DB_FAIL_ON_ERROR=false

# =================================
# Variables de compatibilidad (legacy) - OPCIONAL
# =================================
SA_PASSWORD=TuContraseñaSegura123!
CONNECTION_STRING=Server=sqlserver,1433;Database=WahubManager;User Id=sa;Password=TuContraseñaSegura123!;TrustServerCertificate=true;Encrypt=false

# =================================
# Notas importantes:
# =================================
# 1. La contraseña de SA debe cumplir con los requisitos de complejidad de SQL Server
# 2. Cambia todos los valores por los apropiados para tu entorno
# 3. Para desarrollo local, cambia DB_SERVER por 'localhost'
# 4. Los puertos APP_HTTP_PORT y APP_HTTPS_PORT son los puertos externos
# 5. DB_PORT es el puerto externo para SQL Server

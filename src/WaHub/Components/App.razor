@using Microsoft.AspNetCore.Components.Web
@using Microsoft.AspNetCore.Components.Routing
@inject ILocalizationService Localization
@inject IThemeService ThemeService

<!DOCTYPE html>
<html lang="@Localization.GetString(ResourceKeys.App_Lang)" data-theme="@ThemeService.GetCurrentTheme()">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    @* Temporarily using direct paths instead of @Assets for build to proceed *@
    <link rel="stylesheet" href="lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="css/components.css" />
    <link rel="stylesheet" href="css/wahub-sidebar.css" />
    <link rel="stylesheet" href="WaHub.styles.css" />
    <link rel="stylesheet" href="WaHub.Client.styles.css" />
    @* <ImportMap /> Temporarily commented out for build to proceed *@
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet />
    <title>@Localization.GetString(ResourceKeys.App_Title)</title>
</head>

<body>
    <Routes />
    <script src="_framework/blazor.web.js"></script>
    @* Temporarily using direct path instead of @Assets for build to proceed *@
    <script src="js/app.js"></script>
</body>

</html>

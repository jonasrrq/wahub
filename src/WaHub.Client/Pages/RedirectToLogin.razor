@inject NavigationManager NavManager

@code {
    protected override void OnInitialized() => NavManager.NavigateTo($"/Account/login?returnUrl={Uri.EscapeDataString(NavManager.Uri)}", forceLoad: true);
}
